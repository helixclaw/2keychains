pre-commit:
  parallel: true
  commands:
    typecheck:
      glob: '*.{ts,tsx}'
      run: npm run compile
    lint:
      glob: '*.{ts,tsx,js,json,md}'
      run: npm run lint
      stage_fixed: true
    test:
      glob: '*.{ts,tsx}'
      run: npm run test
